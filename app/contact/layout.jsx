export const metadata = {
  title: 'Contact — Nandini Pillai',
  description: 'Reach out for project inquiries, design consultations, or just to say hello.',
};

export default function ContactLayout({ children }) {
  return children;
}
