export const metadata = {
  title: 'iLancaster — Nandini Pillai',
  description: 'Redesigning the app every Lancaster University student carries daily.',
};

export default function ILancasterLayout({ children }) {
  return children;
}
