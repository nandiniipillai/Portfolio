export const metadata = {
  title: 'Baari — Nandini Pillai',
  description: "A live queue for India's clinics and salons, shipped to production.",
};

export default function BaariLayout({ children }) {
  return children;
}
